-- drop table if exists tarefas;

/*create table tarefas (
	id bigint not null auto_increment,
    descricao varchar(255),
    finalizado bit(1) default 0,
    data_finalizacao date,
    primary key (id)
);
*/

select * from tarefas;